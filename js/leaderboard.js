import{boot}from'./app.js';import{esc}from'./ui.js';
await boot('Leaderboard',async({db})=>{const{data:players=[]}=await db.from('profiles').select('username,display_name,xp,level,rank').eq('leaderboard_opt_in',true).eq('suspended',false).order('xp',{ascending:false}).limit(50);return `<section class="panel"><p class="eyebrow">OPTED-IN PLAYERS ONLY</p><h2>System Rankings</h2><div class="list">${players.map((p,i)=>`<article class="item"><span class="badge">#${i+1} · ${esc(p.rank)}</span><strong>${esc(p.display_name)} <small>@${esc(p.username)}</small></strong><small>LEVEL ${p.level} · ${p.xp} XP</small></article>`).join('')||'<p>No players have joined the rankings.</p>'}</div></section>`});
