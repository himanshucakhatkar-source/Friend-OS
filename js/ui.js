export const esc = (s='') => String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
export function shell(profile, title, content){ return `<aside><a class="brand" href="dashboard.html">DUO <em>SYSTEM</em></a><nav>${[['dashboard.html','Command'],['profile.html','Profile'],['quests.html','Quests'],['friends.html','Allies'],['materials.html','Study Vault'],['achievements.html','Achievements'],['leaderboard.html','Leaderboard']].map(([h,n])=>`<a href="${h}">${n}</a>`).join('')}${profile.role==='admin'?'<a href="admin.html">Administration</a>':''}</nav><button id="logout" class="link-button">Terminate session</button></aside><main class="page"><header><div><p class="eyebrow">PLAYER // ${esc(profile.username)}</p><h1>${title}</h1></div><div class="rank">${profile.rank} RANK <small>LVL ${profile.level}</small></div></header>${content}</main><div id="toast"></div>`; }
export function toast(message, bad=false){ const el=document.querySelector('#toast'); el.textContent=message; el.className=bad?'show bad':'show'; setTimeout(()=>el.className='',3500); }
export function empty(title, text){return `<section class="panel empty"><h2>${title}</h2><p>${text}</p></section>`}
